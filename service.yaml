apiVersion: v1
kind: Service                    
metadata:
  name: tinytrader-server
spec:
  type: NodePort                 
  ports:                         
  - port: 8080
  selector:
    app: tinytrader-server  